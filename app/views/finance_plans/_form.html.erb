<%= form_for(@finance_plan) do |f| %>
  <% if @finance_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finance_plan.errors.count, "error") %> prohibited this finance_plan from being saved:</h2>

      <ul>
      <% @finance_plan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
