<h1>Listing temp_items</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @temp_items.each do |temp_item| %>
      <tr>
      	<td><%= temp_item.id %></td>
      	<td><%= temp_item.created_at %></td>
        <td><%= link_to 'Show', temp_item %></td>
        <td><%= link_to 'Edit', edit_temp_item_path(temp_item) %></td>
        <td><%= link_to 'Destroy', temp_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Temp item', new_temp_item_path %>
