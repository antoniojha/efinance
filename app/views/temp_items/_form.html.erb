<%= form_for(@temp_item) do |f| %>
  <% if @temp_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_item.errors.count, "error") %> prohibited this temp_item from being saved:</h2>

      <ul>
      <% @temp_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
